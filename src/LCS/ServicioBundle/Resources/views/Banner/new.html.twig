{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Banner creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('banner') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
